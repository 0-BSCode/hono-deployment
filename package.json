{
  "name": "hono-multi-deployment",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "test": "vitest --run --passWithNoTests"
  },
  "dependencies": {
    "hono": "^4.6.16"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "vitest": "^2.1.8"
  }
}